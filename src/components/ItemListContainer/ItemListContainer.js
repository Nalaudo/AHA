import React from 'react'
import styles from '../../styles'

function ItemListContainer({greeting}) {
  return (
    <h2 style={styles.itemmomentaneo}>{greeting}</h2>
  )
}

export default ItemListContainer